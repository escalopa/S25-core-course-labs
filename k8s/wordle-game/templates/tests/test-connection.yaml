apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "wordle-game.fullname" . }}-test-connection"
  labels:
    {{- include "wordle-game.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "wordle-game.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
