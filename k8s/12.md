# Lab 12: Kubernetes ConfigMaps

## Overview

Lab 12 focuses on managing non-confidential configuration data in Kubernetes using ConfigMaps. This lab implements:
- Application persistence (visit counter)
- ConfigMap-based file mounting
- ConfigMap-based environment variables

---

## Task 1: Application Persistence

### 1.1 Upgrade Go Application (Wordle Game)

**Modified files:**

- `app_go/main.go` - Added visit counter logic and `/visits` endpoint
- `app_go/README.md` - Updated documentation

**Key Changes:**

- Added `visitCount`, `visitMutex`, and `visitsFilePath` variables
- Implemented `loadVisitCount()` - Loads visit count from `/data/visits` file on startup
- Implemented `saveVisitCount()` - Persists counter to file
- Implemented `incrementVisits()` - Thread-safe counter increment
- Added `VisitsHandler()` - New endpoint returning JSON with visitor count
- Modified `IndexHandler()` - Increments visit count on each game start

**Visits Tracking:**

- File location: `/data/visits`
- Persists across container restarts
- Thread-safe with mutex locks

### 1.2 Upgrade Python Application (Moscow Time)

**Modified files:**

- `app_python/app.py` - Added visit counter logic and `/visits` endpoint
- `app_python/README.md` - Updated documentation

**Key Changes:**

- Added `VISITS_FILE = "/data/visits"`
- Implemented `load_visits()` - Loads counter from file on startup
- Implemented `save_visits()` - Persists counter to file
- Implemented `increment_visits()` - Increments and saves counter
- Added `/visits` endpoint returning JSON with visit count
- Modified root `/` endpoint - Increments counter on each access

### 1.3 Docker Compose Updates

**Updated: `docker-compose.yml`**

Added volume mounts for both applications:

```yaml
volumes:
  - ./app_python/data:/data  # Python app visits
  - ./app_go/data:/data      # Go app visits
```

**Local Testing:**

```bash
# Run with persistence
make compose-up

# Access applications
curl http://localhost:5000          # Moscow Time
curl http://localhost:5000/visits   # Visit count
curl http://localhost:8080          # Wordle Game
curl http://localhost:8080/visits   # Visit count

# Verify file on host
cat app_python/data/visits
cat app_go/data/visits
```

---

## Task 2: ConfigMap Implementation

### 2.1 Create Configuration Files

**Created: `k8s/moscow-time-app/files/config.json`**
```json
{
  "app_name": "Moscow Time Application",
  "version": "1.16.0",
  "environment": "production",
  "log_level": "info",
  "timezone": "Europe/Moscow",
  "api_timeout": 30,
  "port": 5000,
  "features": {
    "cache_enabled": true,
    "persistence": true,
    "metrics": true,
    "health_check": true
  },
  "database": {
    "host": "db.default.svc.cluster.local",
    "port": 5432,
    "pool_size": 10,
    "ssl_mode": "require"
  },
  "monitoring": {
    "prometheus_enabled": true,
    "metrics_port": 9000,
    "endpoint": "/metrics"
  }
}
```

**Created: `k8s/wordle-game/files/config.json`**
```json
{
  "app_name": "Wordle Game Application",
  "version": "1.16.0",
  "environment": "production",
  "log_level": "info",
  "game_config": {
    "max_attempts": 6,
    "word_length": 5,
    "difficulty": "medium"
  },
  "api_timeout": 30,
  "port": 8080,
  "features": {
    "cache_enabled": true,
    "persistence": true,
    "metrics": true,
    "health_check": true
  },
  "database": {
    "host": "db.default.svc.cluster.local",
    "port": 5432,
    "pool_size": 15,
    "ssl_mode": "require"
  },
  "monitoring": {
    "prometheus_enabled": true,
    "metrics_port": 9090,
    "endpoint": "/metrics"
  }
}
```

### 2.2 Create ConfigMap Manifests

**Created: `k8s/moscow-time-app/templates/configmap.yaml`**
```yaml
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "moscow-time-app.fullname" . }}-config
  labels:
    {{- include "moscow-time-app.labels" . | nindent 4 }}
data:
  config.json: |
    {{- .Files.Get "files/config.json" | nindent 4 }}
```

**Created: `k8s/wordle-game/templates/configmap.yaml`**
```yaml
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "wordle-game.fullname" . }}-config
  labels:
    {{- include "wordle-game.labels" . | nindent 4 }}
data:
  config.json: |
    {{- .Files.Get "files/config.json" | nindent 4 }}
```

### 2.3 Update Deployments with Volumes

**Updated Deployments:**

- `k8s/moscow-time-app/templates/deployment.yaml`
- `k8s/wordle-game/templates/deployment.yaml`

Added volume mount and volume definitions:
```yaml
volumeMounts:
  - name: config
    mountPath: /config
    readOnly: true

volumes:
  - name: config
    configMap:
      name: {{ include "chart.fullname" . }}-config
      items:
        - key: config.json
          path: config.json
          mode: 0644
```

### 2.4 ConfigMap Verification

**ConfigMaps Created:**

```bash
$ kubectl get configmap
NAME                                 DATA   AGE
moscow-time-moscow-time-app-config   1      12m
wordle-game-config                   1      12m
```

**Moscow Time ConfigMap Content:**

```yaml
apiVersion: v1
kind: ConfigMap
metadata:
  name: moscow-time-moscow-time-app-config
  namespace: default
data:
  config.json: |
    {
      "app_name": "Moscow Time Application",
      "version": "1.16.0",
      "environment": "production",
      "log_level": "info",
      "timezone": "Europe/Moscow",
      "api_timeout": 30,
      "port": 5000,
      "features": {
        "cache_enabled": true,
        "persistence": true,
        "metrics": true,
        "health_check": true
      },
      "database": {
        "host": "db.default.svc.cluster.local",
        "port": 5432,
        "pool_size": 10,
        "ssl_mode": "require"
      },
      "monitoring": {
        "prometheus_enabled": true,
        "metrics_port": 9000,
        "endpoint": "/metrics"
      }
    }
```

**Verify Config File in Pod:**

```bash
$ kubectl exec moscow-time-moscow-time-app-89849c67f-6br7v -- cat /config/config.json
{
  "app_name": "Moscow Time Application",
  "version": "1.16.0",
  ...
}
```

---

## Bonus Task: ConfigMap Environment Variables

### Bonus 1: Create Environment ConfigMaps

**Created: `k8s/moscow-time-app/templates/configmap-env.yaml`**
```yaml
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "moscow-time-app.fullname" . }}-env
data:
  APP_CONFIG_ENV: "production"
  LOG_LEVEL: "info"
  FEATURE_FLAG_CACHE: "true"
  FEATURE_FLAG_PERSISTENCE: "true"
  FEATURE_FLAG_METRICS: "true"
  DB_CONNECTION_POOL_SIZE: "10"
  API_TIMEOUT: "30"
  SERVICE_PORT: "5000"
```

**Created: `k8s/wordle-game/templates/configmap-env.yaml`**
```yaml
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "wordle-game.fullname" . }}-env
data:
  APP_CONFIG_ENV: "production"
  LOG_LEVEL: "info"
  FEATURE_FLAG_CACHE: "true"
  FEATURE_FLAG_PERSISTENCE: "true"
  FEATURE_FLAG_METRICS: "true"
  DB_CONNECTION_POOL_SIZE: "15"
  API_TIMEOUT: "30"
  SERVICE_PORT: "8080"
  GAME_MAX_ATTEMPTS: "6"
  GAME_DIFFICULTY: "medium"
```

### Bonus 2: Update Deployments with envFrom

Updated deployment files with:
```yaml
envFrom:
  - configMapRef:
      name: {{ include "chart.fullname" . }}-env
```

### Bonus 3: Environment ConfigMaps Verification

**All ConfigMaps:**

```bash
$ kubectl get configmap
NAME                                 DATA   AGE
moscow-time-moscow-time-app-config   1      12m
moscow-time-moscow-time-app-env      8      4m27s
wordle-game-config                   1      12m
wordle-game-env                      10     4m26s
```

**Moscow Time Environment ConfigMap:**

```yaml
apiVersion: v1
kind: ConfigMap
metadata:
  name: moscow-time-moscow-time-app-env
data:
  API_TIMEOUT: "30"
  APP_CONFIG_ENV: production
  DB_CONNECTION_POOL_SIZE: "10"
  FEATURE_FLAG_CACHE: "true"
  FEATURE_FLAG_METRICS: "true"
  FEATURE_FLAG_PERSISTENCE: "true"
  LOG_LEVEL: info
  SERVICE_PORT: "5000"
```

**Wordle Game Environment ConfigMap:**

```yaml
apiVersion: v1
kind: ConfigMap
metadata:
  name: wordle-game-env
data:
  API_TIMEOUT: "30"
  APP_CONFIG_ENV: production
  DB_CONNECTION_POOL_SIZE: "15"
  FEATURE_FLAG_CACHE: "true"
  FEATURE_FLAG_METRICS: "true"
  FEATURE_FLAG_PERSISTENCE: "true"
  GAME_DIFFICULTY: medium
  GAME_MAX_ATTEMPTS: "6"
  LOG_LEVEL: info
  SERVICE_PORT: "8080"
```

---

## Summary of Changes

### Applications
✅ **Go Application (`app_go/`):**

- Added visit counter with persistence
- New `/visits` endpoint returning JSON
- Implemented thread-safe file I/O
- Updated README with persistence documentation

✅ **Python Application (`app_python/`):**

- Added visit counter with persistence
- New `/visits` endpoint returning JSON
- Implemented global counter management
- Updated README with persistence documentation

### Kubernetes - ConfigMaps for Files
✅ **Moscow Time App:**

- `k8s/moscow-time-app/files/config.json` - Configuration file
- `k8s/moscow-time-app/templates/configmap.yaml` - ConfigMap manifest
- Updated `deployment.yaml` - Volume mount at `/config`

✅ **Wordle Game App:**

- `k8s/wordle-game/files/config.json` - Configuration file
- `k8s/wordle-game/templates/configmap.yaml` - ConfigMap manifest
- Updated `deployment.yaml` - Volume mount at `/config`

### Kubernetes - ConfigMaps for Environment Variables
✅ **Moscow Time App:**

- `templates/configmap-env.yaml` - Environment variables ConfigMap (8 keys)
- Updated `deployment.yaml` - `envFrom` reference

✅ **Wordle Game App:**

- `templates/configmap-env.yaml` - Environment variables ConfigMap (10 keys)
- Updated `deployment.yaml` - `envFrom` reference

### Docker Compose
✅ Updated `docker-compose.yml` with volume mounts for both applications

---

## Testing Procedures

### Local Testing (Docker Compose)
```bash
# Run applications with volumes
make compose-up

# Test moscow-time visits
curl http://localhost:5000/visits
curl http://localhost:5000/visits  # visit count increments

# Test wordle-game visits
curl http://localhost:8080/visits
curl http://localhost:8080/visits  # visit count increments

# Verify persistence files on host
cat app_python/data/visits
cat app_go/data/visits

# Stop and restart
make compose-down
make compose-up

# Verify counter persisted
curl http://localhost:5000/visits  # Shows cumulative count
```

### Kubernetes Testing
```bash
# List ConfigMaps
kubectl get configmap

# View ConfigMap content
kubectl get configmap moscow-time-moscow-time-app-config -o yaml

# Read config file in pod
kubectl exec <pod-name> -- cat /config/config.json

# Check environment variables (after pod restart)
kubectl exec <pod-name> -- env | grep -E "^(FEATURE|DB_|LOG_|APP_CONFIG)"

# Describe ConfigMaps
kubectl describe configmap <name>
```

---

## Files Created/Modified

### New Files
- `k8s/moscow-time-app/files/config.json`
- `k8s/moscow-time-app/templates/configmap.yaml`
- `k8s/moscow-time-app/templates/configmap-env.yaml`
- `k8s/wordle-game/files/config.json`
- `k8s/wordle-game/templates/configmap.yaml`
- `k8s/wordle-game/templates/configmap-env.yaml`

### Modified Files
- `app_go/main.go` - Added visit counter
- `app_go/README.md` - Updated documentation
- `app_python/app.py` - Added visit counter
- `app_python/README.md` - Updated documentation
- `docker-compose.yml` - Added volume mounts
- `k8s/moscow-time-app/templates/deployment.yaml` - Added volumes and envFrom
- `k8s/wordle-game/templates/deployment.yaml` - Added volumes and envFrom

---

## Requirements Completion

✅ **Task 1 (6 Points):**

- ✓ Application visit counter implementing persistence
- ✓ `/visits` endpoint in both apps
- ✓ Docker-compose volume configuration
- ✓ Updated README files with persistence documentation
- ✓ Local testing verified and documented

✅ **Task 2 (4 Points):**

- ✓ `config.json` files in `files` folder (both charts)
- ✓ ConfigMap manifests using `.Files.Get`
- ✓ Volumes and VolumeMounts in deployments
- ✓ Configuration file accessible in pods
- ✓ ConfigMaps successfully created and verified

✅ **Bonus (2.5 Points):**

- ✓ ConfigMap environment variables implemented
- ✓ `envFrom` properties added to deployments
- ✓ Verified ConfigMap environment injection

---

## Conclusion

Lab 12 successfully implements:
1. **Application Persistence:**
 Visit counters persisting across container restarts
2. **ConfigMap File Mounting:**
 Configuration files mounted at `/config` path
3. **ConfigMap Environment Variables:**
 Using `envFrom` for environment configuration
4. **Documentation:**
 Comprehensive documentation with verification outputs
5. **Best Practices:**
 Separate concerns between config files and environment variables
